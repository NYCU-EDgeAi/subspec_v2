# SubSpec SD (v2 FI) trace config.
# Mirrors subspec_sd_v2_fi.yaml (compile already disabled there); enables step
# tracing and re-enables post-verify so divergences can be traced.
extends: subspec_sd_v2_fi.yaml

generator_kwargs:
  disable_post_verify: false
  step_trace: true
