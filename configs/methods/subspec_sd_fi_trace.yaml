# SubSpec SD FI deterministic trace.
# Mirrors subspec_sd_fi.yaml; only disables compile and enables step tracing.
extends: subspec_sd_fi.yaml
compile_mode: null

generator_kwargs:
  step_trace: true
