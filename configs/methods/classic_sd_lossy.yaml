# Classic SD + lossy verification. Inherits classic_sd.yaml; only max_length and the
# verifier differ.
extends: classic_sd.yaml

max_length: 2048

generator_kwargs:
  verify_method: lossy
  verify_kwargs:
    threshold_method: entropy
    threshold: 0.3
    window_size: 6
