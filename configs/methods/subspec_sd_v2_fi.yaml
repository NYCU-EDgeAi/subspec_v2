# SubSpec SD (v2 FI) method config
method: subspec_sd_v2_fi

llm_path: meta-llama/Llama-3.1-8B-Instruct

recipe:
  class_path: specdecodes.helpers.recipes.subspec.hqq_4bit_postspec:Recipe

max_length: 4096
cache_implementation: static
warmup_iter: 1
# flashinfer requires eager mode; torch.compile is disabled
compile_mode: null



# Target sampling settings (defaults)
do_sample: false
temperature: 0

generator_profiling: true

generator_kwargs:
  prefill_chunk_size: 256
  verify_method: exact
  verify_kwargs: {}
  disable_post_verify: true

draft_params:
  temperature: 0.2
  max_depth: 32
  topk_len: 6
